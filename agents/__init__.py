"""ADK agents directory.

Each subdirectory that exposes a ``root_agent`` in its ``agent`` module is an
ADK "app" discoverable by both ``adk web agents`` and
``get_fast_api_app(agents_dir="agents")``. The only app here is ``cleo``.
"""
